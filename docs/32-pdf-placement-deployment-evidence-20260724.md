# PDF配置連携・本番生成確認（2026-07-24）

## 確認できたこと

- 固定Apps Script Webアプリへ、タグ定義と配置定義（x・y・幅・高さ・文字サイズ）を渡せるようにした。
- 工事完了報告書で、配置定義を含むリクエストから1ページPDFを生成し、Google Driveへ保存できた。
- 生成ファイル: `verification-completion-exact(4).pdf`
- Drive file ID: `1z_LZ3slqxm3yG5m4wNV-Jck1Mdl5TKQ4`
- 生成PDFを画像化して確認し、台紙、文字、丸印、領収書画像、改行表示が現行参照と同じ見た目になることを確認した。
- 丸産報告書は、完了報告書のタグを流用せず、`ms_`で始まる丸産専用タグを使う経路で生成確認済み。

## まだ本番化できない項目

- 入力データとタグ設定は現在ブラウザ内保存（localStorage）で、複数端末の共有保存ではない。
- 認証付きの利用者・管理者・閲覧者権限、操作履歴、通知は未接続。
- 領収書の加工後画像をGoogle Driveへ永続保存する処理は未接続。
- 最新のローカル変更はまだGitHub Pagesへ公開していない。公開にはコミット・プッシュの承認が必要。

## 判定

PDF生成の中心経路は、現行の「フォーム入力 → Google Slides台紙 → タグ置換・画像配置 → PDF化 → Drive保存」に接続できた。現行PDFとの確認は見た目の受入であり、PDFバイナリの完全一致までは保証しない。
