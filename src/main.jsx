import { StrictMode, useEffect, useState } from 'react'
import { createRoot } from 'react-dom/client'
import {
  Archive, ArrowLeft, ArrowRight, Check, ChevronDown, ClipboardList, Cloud,
  FileCheck2, FileText, ImagePlus, LayoutDashboard, Menu, MoreHorizontal,
  Plus, ReceiptText, RotateCw, Save, Settings2, ShieldCheck, SlidersHorizontal,
  Sparkles, WandSparkles,
} from 'lucide-react'
import { templateConfig } from './config/template-config'
import './styles.css'

const storageKey = 'oshigoto-prototype-records'
const completionSeed = { id: 'completion-draft-001', type: 'completion', status: 'draft', siteName: '港南区マンション外壁改修', orderNo: 'TRC-2026-071', workDate: '2026-07-22', address: '神奈川県横浜市港南区', toCompany: '株式会社〇〇', toOffice: '工事部', toPerson: 'ご担当者様', workContent: '外壁面の補修・清掃\n既存塗膜の確認と下地処理', total: '128000', tax: '12800', spray: 'あり', sprayDetail: '外壁西面を2回吹き付け\n仕上がりを現場確認', specialMaterial: '養生材、下地補修材を使用', completionStatus: '完了', receiptImages: [] }
const marusanSeed = { id: 'marusan-draft-001', type: 'marusan', status: 'draft', siteName: '丸産技研 現場A', personInCharge: '富田', year: '2026', month: '7', day: '22', amTime: '8:00 - 12:00', pmTime: '13:00 - 17:00', workSlots: ['足場まわり確認', '外壁補修', '清掃', '塗装下準備', '', ''], workers: ['富田', '佐藤', '鈴木', '', '', ''], notes: '安全確認を実施。明日は仕上げ確認予定。' }

function loadDrafts() { try { return JSON.parse(localStorage.getItem(storageKey)) || {} } catch { return {} } }
function saveDrafts(value) { localStorage.setItem(storageKey, JSON.stringify(value)) }

function App() {
  const [page, setPage] = useState('home')
  const [drafts, setDrafts] = useState(loadDrafts)
  const [activeForm, setActiveForm] = useState(null)
  const [notice, setNotice] = useState('')
  const [menuOpen, setMenuOpen] = useState(false)
  useEffect(() => { if (!notice) return; const timer = setTimeout(() => setNotice(''), 3200); return () => clearTimeout(timer) }, [notice])
  const openForm = (type, existing) => { const seed = type === 'completion' ? completionSeed : marusanSeed; setActiveForm({ ...seed, ...(existing || {}) }); setPage(`${type}-form`); setMenuOpen(false) }
  const update = (next) => { setActiveForm(next); const value = { ...drafts, [next.id]: next }; setDrafts(value); saveDrafts(value) }
  const save = () => { if (!activeForm) return; update({ ...activeForm, updatedAt: new Date().toLocaleString('ja-JP', { hour12: false }) }); setNotice('下書きを保存しました') }
  const complete = () => { if (!activeForm) return; update({ ...activeForm, status: 'processing' }); setPage('jobs'); setNotice('PDF作成を受け付けました。サンプル環境で処理中です。') }
  const draftCount = Object.keys(drafts).length
  const processingCount = Object.values(drafts).filter(item => item.status === 'processing').length
  return <div className="app-shell">
    <aside className={`sidebar ${menuOpen ? 'is-open' : ''}`}><div className="brand-lockup"><div className="brand-mark"><Sparkles size={18} /></div><div><strong>オシゴト</strong><span>帳票ワークスペース</span></div></div><div className="environment-pill"><i />サンプル環境</div><nav><NavButton active={page === 'home'} onClick={() => setPage('home')} icon={<LayoutDashboard size={18} />}>ホーム</NavButton><NavButton active={page === 'drafts'} onClick={() => setPage('drafts')} icon={<Archive size={18} />}>下書き <b>{draftCount}</b></NavButton><NavButton active={page === 'jobs'} onClick={() => setPage('jobs')} icon={<Cloud size={18} />}>PDF処理</NavButton><hr /><NavButton active={page === 'settings'} onClick={() => setPage('settings')} icon={<Settings2 size={18} />}>設定</NavButton></nav><div className="sidebar-footer"><div className="avatar">K</div><div><strong>kmk7531</strong><span>管理者・試作ユーザー</span></div><ChevronDown size={16} /></div></aside>
    <main className="main-content"><header className="topbar"><button className="icon-button mobile-menu" aria-label="メニュー" onClick={() => setMenuOpen(!menuOpen)}><Menu size={20} /></button><div className="breadcrumb">オシゴトアプリ <ChevronDown size={15} /></div><div className="topbar-actions"><span className="sync-state"><i />ローカル保存</span><button className="icon-button" aria-label="その他" onClick={() => setMenuOpen(!menuOpen)}><MoreHorizontal size={20} /></button></div></header>{notice && <div className="toast"><Check size={16} />{notice}</div>}
      {page === 'home' && <Home create={openForm} go={setPage} draftCount={draftCount} processingCount={processingCount} />}
      {page === 'drafts' && <Drafts drafts={drafts} open={openForm} />}
      {page === 'jobs' && <Jobs drafts={drafts} back={() => setPage('home')} />}
      {page === 'settings' && <SettingsPage />}
      {page === 'completion-form' && activeForm && <CompletionForm data={activeForm} update={update} back={() => setPage('home')} save={save} complete={complete} receipt={() => setPage('receipt')} />}
      {page === 'marusan-form' && activeForm && <MarusanForm data={activeForm} update={update} back={() => setPage('home')} save={save} complete={complete} />}
      {page === 'receipt' && activeForm && <ReceiptEditor data={activeForm} update={update} back={() => setPage('completion-form')} done={() => { setPage('completion-form'); setNotice('領収書を保存しました') }} />}
    </main></div>
}

function NavButton({ active, onClick, icon, children }) { return <button className={`nav-item ${active ? 'active' : ''}`} onClick={onClick}>{icon}{children}</button> }
function Header({ eyebrow, title, text, action }) { return <div className="page-header"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p>{text}</p></div>{action}</div> }

function Home({ create, go, draftCount, processingCount }) { return <div className="page-wrap"><Header eyebrow="今日の仕事" title="おつかれさまです、富田さん" text="現場の記録から帳票作成まで、ここから進められます。" action={<button className="button primary" onClick={() => create('completion')}><Plus size={17} />新しい報告書</button>} /><section className="hero-band"><div><div className="hero-kicker"><Sparkles size={15} />仕事の流れをひとつに</div><h2>入力した内容を、<br />そのまま帳票へ。</h2><p>下書き保存と処理状況が見えるので、現場から事務所まで迷いません。</p></div><div className="hero-orbit"><FileCheck2 size={54} /><span>PDF</span></div></section><div className="section-heading"><h2>すぐに作成</h2><span>よく使う帳票を選択してください</span></div><div className="create-grid"><CreateCard tone="teal" icon={<FileText size={23} />} title="工事完了報告書" text="現場・工事内容・金額・領収書" onClick={() => create('completion')} /><CreateCard tone="orange" icon={<ClipboardList size={23} />} title="丸産報告書" text="作業日・AM/PM・作業者" onClick={() => create('marusan')} /></div><div className="dashboard-grid"><Stat icon={<Archive size={17} />} title="下書き" value={String(draftCount)} unit="件" text="続きを入力する" onClick={() => go('drafts')} /><Stat icon={<Cloud size={17} />} title="PDF処理" value={String(processingCount)} unit="件" text="作成状況を確認する" onClick={() => go('jobs')} /><Stat quiet icon={<ShieldCheck size={17} />} title="保存状態" value="安定" text="この端末に保存" /></div></div> }
function CreateCard({ tone, icon, title, text, onClick }) { return <button className={`create-card ${tone}`} onClick={onClick}><div className="card-icon">{icon}</div><div><strong>{title}</strong><span>{text}</span></div><ArrowRight size={19} /></button> }
function Stat({ icon, title, value, unit, text, onClick, quiet }) { return <button className={`stat-card ${quiet ? 'quiet' : ''}`} onClick={onClick}><div className="stat-label">{icon}{title}</div><strong>{value}{unit && <small>{unit}</small>}</strong><span>{text}</span><ArrowRight size={17} /></button> }

function Drafts({ drafts, open }) { const list = Object.values(drafts); return <div className="page-wrap"><Header eyebrow="作業中の書類" title="下書き一覧" text="入力途中の書類はここから再開できます。" action={<button className="button primary" onClick={() => open('completion')}><Plus size={17} />新規作成</button>} /><div className="filter-row"><button className="filter-chip active">すべて <b>{list.length}</b></button><button className="filter-chip">完了報告書</button><button className="filter-chip">丸産報告書</button></div><div className="record-list">{list.length ? list.map(item => <RecordRow item={item} key={item.id} onClick={() => open(item.type, item)} />) : <Empty />}</div></div> }
function RecordRow({ item, onClick }) { const marusan = item.type === 'marusan'; return <button className="record-row" onClick={onClick}><div className={`record-type ${marusan ? 'orange' : 'teal'}`}>{marusan ? <ClipboardList size={19} /> : <FileText size={19} />}</div><div className="record-main"><strong>{item.siteName || '未入力の書類'}</strong><span>{marusan ? '丸産報告書' : '工事完了報告書'} <i>·</i> {item.updatedAt || '保存済み'}</span></div><span className={`status-label ${item.status || 'draft'}`}>{item.status === 'processing' ? '作成中' : item.status === 'completed' ? '作成済み' : '下書き'}</span><ArrowRight size={18} /></button> }
function Empty() { return <div className="empty-state"><Archive size={28} /><strong>下書きはありません</strong><span>新しい報告書を作成すると、ここに表示されます。</span></div> }

function Jobs({ drafts, back }) { const processing = Object.values(drafts).filter(item => item.status === 'processing'); return <div className="page-wrap"><Header eyebrow="処理状況" title="PDF処理" text="帳票の作成状況を確認できます。" action={<button className="button secondary" onClick={back}><ArrowLeft size={17} />ホームへ戻る</button>} /><div className="job-summary"><div><span>現在の処理</span><strong>{processing.length ? '1件を作成中' : '処理はありません'}</strong></div><div className="job-spinner"><Cloud size={22} /></div></div><div className="record-list">{processing.length ? processing.map(item => <RecordRow item={item} key={item.id} onClick={() => {}} />) : <Empty />}</div><div className="info-callout"><Sparkles size={18} /><div><strong>サンプル環境で動作中</strong><span>本番のDriveやSheetsには接続していません。まずは画面と入力フローを確認できます。</span></div></div></div> }

function Field({ label, required, hint, children, className = '' }) { return <label className={`field ${className}`}><span className="field-label">{label}{required && <em>必須</em>}</span>{children}{hint && <small>{hint}</small>}</label> }
function Input({ value, onChange, type = 'text', placeholder }) { return <input className="text-input" type={type} value={value ?? ''} placeholder={placeholder} onChange={e => onChange(e.target.value)} /> }
function Area({ value, onChange, rows = 4, placeholder }) { return <textarea className="text-input textarea" rows={rows} value={value ?? ''} placeholder={placeholder} onChange={e => onChange(e.target.value)} /> }
function FormShell({ eyebrow, title, text, back, save, complete, step, children }) { return <div className="page-wrap form-page"><div className="form-top"><button className="back-link" onClick={back}><ArrowLeft size={17} />一覧へ戻る</button><span className="form-step">{step}</span></div><Header eyebrow={eyebrow} title={title} text={text} /><div className="form-layout"><div className="form-body">{children}</div><aside className="form-side"><div className="side-card"><div className="side-icon"><Save size={18} /></div><strong>下書き保存</strong><span>入力途中でも、いつでも保存できます。</span><button className="button secondary full" onClick={save}>保存する</button></div><div className="side-card pale"><div className="side-icon"><FileCheck2 size={18} /></div><strong>確認してPDF作成</strong><span>入力内容を確認してから帳票を作成します。</span><button className="button primary full" onClick={complete}>完了してPDF作成</button></div></aside></div></div> }

function CompletionForm({ data, update, back, save, complete, receipt }) { const patch = (key, value) => update({ ...data, [key]: value }); return <FormShell eyebrow="工事完了報告書" title="完了報告書を作成" text="現場の内容を入力してください。長い文章は枠に合わせて自動で折り返されます。" back={back} save={save} complete={complete} step="01 / 02 基本情報"><Section title="基本情報" note="書類の宛先と現場"><div className="form-grid two"><Field label="宛先会社" required><Input value={data.toCompany} onChange={v => patch('toCompany', v)} /></Field><Field label="部署"><Input value={data.toOffice} onChange={v => patch('toOffice', v)} /></Field><Field label="担当者"><Input value={data.toPerson} onChange={v => patch('toPerson', v)} /></Field><Field label="作業日" required><Input type="date" value={data.workDate} onChange={v => patch('workDate', v)} /></Field><Field label="現場名" required><Input value={data.siteName} onChange={v => patch('siteName', v)} /></Field><Field label="注文番号"><Input value={data.orderNo} onChange={v => patch('orderNo', v)} /></Field><Field label="住所" className="span-two"><Input value={data.address} onChange={v => patch('address', v)} /></Field></div></Section><Section title="工事内容" note="改行はPDFでも保持されます"><Field label="工事内容" required><Area value={data.workContent} onChange={v => patch('workContent', v)} rows={5} placeholder="行ごとに工事内容を入力" /></Field><div className="inline-note"><Check size={15} />数量は先頭行のみ「一式」で固定。備考欄は空欄で出力します。</div></Section><Section title="金額" note="駐車場代・高速代・材料代は含めません"><div className="form-grid two"><Field label="合計金額" required hint="税抜の金額を入力"><Input type="number" value={data.total} onChange={v => update({ ...data, total: v, tax: v ? Math.floor(Number(v) * .1) : '' })} /></Field><Field label="消費税" hint="10%を自動計算・手動編集可"><Input type="number" value={data.tax} onChange={v => patch('tax', v)} /></Field></div></Section><Section title="仕上がり" note="PDF上で選択項目に丸印を表示"><Field label="吹付"><Segment value={data.spray} options={['あり', 'なし']} onChange={v => patch('spray', v)} /></Field>{data.spray === 'あり' && <Field label="吹付詳細"><Area value={data.sprayDetail} onChange={v => patch('sprayDetail', v)} rows={4} placeholder="吹付の内容を入力" /></Field>}<Field label="通常材料以外の材料"><Area value={data.specialMaterial} onChange={v => patch('specialMaterial', v)} rows={3} placeholder="該当する材料があれば入力" /></Field><Field label="完了状態"><Segment value={data.completionStatus} options={['完了', '未完']} onChange={v => patch('completionStatus', v)} /></Field></Section><Section title="領収書" note="任意・複数枚対応予定"><button className="receipt-launch" onClick={receipt} type="button"><div className="receipt-icon"><ReceiptText size={22} /></div><div><strong>{data.receiptImages?.length ? `${data.receiptImages.length}枚の領収書を確認` : '領収書を追加・加工'}</strong><span>撮影・画像選択、トリミング、白黒化</span></div><ArrowRight size={18} /></button></Section></FormShell> }

function Section({ title, note, children }) { return <div className="form-section"><div className="form-section-title"><span>{title}</span><small>{note}</small></div>{children}</div> }
function Segment({ value, options, onChange }) { return <div className="segmented">{options.map(option => <button type="button" key={option} className={value === option ? 'selected' : ''} onClick={() => onChange(option)}>{option}</button>)}</div> }

function MarusanForm({ data, update, back, save, complete }) { const patch = (key, value) => update({ ...data, [key]: value }); const arrayPatch = (key, index, value) => { const next = [...data[key]]; next[index] = value; update({ ...data, [key]: next }) }; return <FormShell eyebrow="丸産報告書" title="丸産報告書を作成" text="作業日、時間帯、作業内容、作業者を入力してください。" back={back} save={save} complete={complete} step="01 / 02 作業日報"><Section title="作業情報" note="会社名は固定です"><div className="fixed-company"><ShieldCheck size={17} /><span>株式会社ＴＲＣ</span><small>固定</small></div><div className="form-grid two"><Field label="担当者"><Input value={data.personInCharge} onChange={v => patch('personInCharge', v)} /></Field><Field label="現場名" required><Input value={data.siteName} onChange={v => patch('siteName', v)} /></Field><Field label="年" required><Input type="number" value={data.year} onChange={v => patch('year', v)} /></Field><Field label="月" required><Input type="number" value={data.month} onChange={v => patch('month', v)} /></Field><Field label="日" required><Input type="number" value={data.day} onChange={v => patch('day', v)} /></Field></div></Section><Section title="時間帯" note="AM / PMを自由入力"><div className="form-grid two"><Field label="AM"><Input value={data.amTime} onChange={v => patch('amTime', v)} placeholder="例：8:00 - 12:00" /></Field><Field label="PM"><Input value={data.pmTime} onChange={v => patch('pmTime', v)} placeholder="例：13:00 - 17:00" /></Field></div></Section><Section title="作業内容" note="6行でPDFへ出力"><div className="repeat-fields">{data.workSlots.map((value, index) => <Field key={index} label={`${index < 3 ? 'AM' : 'PM'} ${index % 3 + 1}`}><Input value={value} onChange={v => arrayPatch('workSlots', index, v)} placeholder="作業内容" /></Field>)}</div></Section><Section title="作業者" note="最初の行は本人として固定予定"><div className="repeat-fields">{data.workers.map((value, index) => <Field key={index} label={`作業者 ${index + 1}`}><Input value={value} onChange={v => arrayPatch('workers', index, v)} placeholder={index === 0 ? '本人の名前' : '作業者名'} /></Field>)}</div></Section><Section title="備考" note="改行はPDFでも保持されます"><Field label="備考・注意事項・翌日の予定"><Area value={data.notes} onChange={v => patch('notes', v)} rows={5} placeholder="自由に入力してください" /></Field></Section></FormShell> }

function ReceiptEditor({ data, update, back, done }) { const [selected, setSelected] = useState(data.receiptImages?.[0] || null); const [filter, setFilter] = useState('original'); const [zoom, setZoom] = useState(100); const [rotation, setRotation] = useState(0); const [crop, setCrop] = useState(0); const addFile = files => { const file = files?.[0]; if (!file) return; const item = { id: `receipt-${Date.now()}`, name: file.name, url: URL.createObjectURL(file), filter: 'original', rotation: 0, crop: 0 }; setSelected(item); update({ ...data, receiptImages: [...(data.receiptImages || []), item] }) }; const changeItem = changes => { if (!selected) return; const next = { ...selected, ...changes }; setSelected(next); update({ ...data, receiptImages: data.receiptImages.map(item => item.id === next.id ? next : item) }) }; const imageStyle = { transform: `rotate(${rotation}deg) scale(${zoom / 100})`, filter: filter === 'bw' ? 'grayscale(1) contrast(1.25)' : filter === 'soft' ? 'grayscale(.7) contrast(1.1)' : 'none' }; return <div className="page-wrap receipt-page"><div className="form-top"><button className="back-link" onClick={back}><ArrowLeft size={17} />完了報告書へ戻る</button><span className="form-step">領収書編集</span></div><Header eyebrow="領収書" title="領収書を整える" text="PDFへ貼り付ける前に、見やすさと範囲を確認できます。" action={<button className="button primary" onClick={done}><Check size={17} />保存して戻る</button>} /><div className="receipt-layout"><div className="receipt-canvas"><div className="canvas-label"><span>プレビュー</span><small>PDF貼り付け枠</small></div><div className="receipt-preview">{selected ? <img src={selected.url} alt="領収書プレビュー" style={imageStyle} /> : <div className="receipt-empty"><ReceiptText size={34} /><strong>領収書を追加してください</strong><span>撮影または画像ファイルを選択できます</span></div>}</div></div><div className="receipt-controls"><div className="control-block"><div className="control-title">画像を追加</div><label className="upload-button"><ImagePlus size={18} />撮影・画像を選択<input type="file" accept="image/*" capture="environment" onChange={e => addFile(e.target.files)} /></label><small>元画像は保持し、PDF用の加工画像を別に扱います。</small></div><div className="control-block"><div className="control-title">見やすさ</div><Segment value={filter === 'original' ? '原本' : filter === 'soft' ? 'ソフト白黒' : '白黒・高コントラスト'} options={['原本', 'ソフト白黒', '白黒・高コントラスト']} onChange={v => { const next = v === '原本' ? 'original' : v === 'ソフト白黒' ? 'soft' : 'bw'; setFilter(next); changeItem({ filter: next }) }} /></div><div className="control-block"><div className="control-title">位置調整</div><label className="range-label">拡大 <output>{zoom}%</output><input type="range" min="80" max="140" value={zoom} onChange={e => setZoom(Number(e.target.value))} /></label><label className="range-label">トリミング <output>{crop}%</output><input type="range" min="0" max="30" value={crop} onChange={e => { const v = Number(e.target.value); setCrop(v); changeItem({ crop: v }) }} /></label><button className="button secondary full" onClick={() => { const next = (rotation + 90) % 360; setRotation(next); changeItem({ rotation: next }) }}><RotateCw size={16} />90度回転</button></div>{data.receiptImages?.length > 0 && <div className="control-block"><div className="control-title">添付済み <span>{data.receiptImages.length}枚</span></div>{data.receiptImages.map(item => <button key={item.id} className={`attachment-item ${selected?.id === item.id ? 'active' : ''}`} onClick={() => { setSelected(item); setFilter(item.filter || 'original'); setRotation(item.rotation || 0); setCrop(item.crop || 0) }}><img src={item.url} alt="" /><span>{item.name}</span><Check size={15} /></button>)}</div>}</div></div></div> }

function SettingsPage() { return <div className="page-wrap"><Header eyebrow="管理者向け" title="設定" text="帳票と運用の設定を管理します。試作段階では表示のみです。" /><div className="settings-grid"><Setting icon={<SlidersHorizontal size={20} />} title="帳票レイアウト" text="PDFの枠、位置、文字設定を管理" tone="teal" /><Setting icon={<ShieldCheck size={20} />} title="ユーザーと権限" text="管理者・利用者・閲覧者を管理" tone="orange" /><Setting icon={<WandSparkles size={20} />} title="マスタデータ" text="会社名、材料、交通費を管理" tone="blue" /></div><div className="config-preview"><div className="section-heading"><h2>現在のレイアウト設定</h2><span>位置調整用の設定を帳票ごとに分離</span></div><div className="config-list">{Object.values(templateConfig).map(config => <div className="config-row" key={config.label}><strong>{config.label}</strong><span>{config.templateVersion || config.source}</span><span>{Object.keys(config.fields || {}).length ? `${Object.keys(config.fields).length}項目` : '添付ルール'}</span></div>)}</div></div><div className="info-callout"><Settings2 size={18} /><div><strong>変更しやすい構造で準備中</strong><span>タグ位置や帳票仕様は、アプリ本体から分離した設定として管理する予定です。</span></div></div></div> }
function Setting({ icon, title, text, tone }) { return <div className="settings-card"><div className={`settings-card-icon ${tone}`}>{icon}</div><div><strong>{title}</strong><span>{text}</span></div><em>次の段階</em></div> }

createRoot(document.getElementById('root')).render(<StrictMode><App /></StrictMode>)
